Source:        Forwarded to Vault Data Servers via BonChat operator broadcast
Operator-broadcast date: 2026-05-14 (per source recall)
Source-relay timestamp:  2026-05-14 18:59 MST (when TJ forwarded into investigator's chat)
Channel:       BonChat — BG-015 cohort (same as prior HQIEX-pivot broadcasts)
Sequence:      Same-day rotation following the hqi18/19 push earlier on
               2026-05-14. The 2026-05-13 initial rebrand (hqi22/23) had
               been Cloudflare-flagged by this point.

               EARLIER NOTE WITHDRAWN: An earlier version of this file
               claimed all four HQIEX broadcasts went out in an 11-minute
               window with a 44% reserve burn. That timing was wrong — it
               reflected the gaps between source-relay messages, not
               operator-broadcast intervals. Actual operator-broadcast
               cadence: 2026-05-13 (rebrand) → 2026-05-14 (rotations).

---

HQI Platform Registration Links:

hqi15.com/h5/ios#/login

hqi16.com/h5/ios#/login

hqi17.com/h5/ios#/login

Invitation Code: CBK806SRN400

---

Analysis (per investigator):

1. **URL-path change to `#/login` (was `#/home`).** Operators corrected the
   landing target after observing that victims need to authenticate, not
   land on a generic home view. Indicates operators are watching real-time
   victim behavior across the rollout.

2. **Live HTTP probe at broadcast time (2026-05-14 18:59 MST + ~1 min):**
   - hqi15.com  HTTP 200 (clean SPA shell)
   - hqi16.com  HTTP 403 — Cloudflare "Suspected phishing site"
   - hqi17.com  HTTP 200 (clean SPA shell)
   **Three of the seven broadcast domains so far (hqi22, hqi23, hqi16) are
   Cloudflare-flagged within hours/minutes of being made operational.**
   Operators broadcast hqi16 without pre-checking — confirms they are not
   testing URLs before pushing to the BG-015 channel.

3. **Cloudflare auto-detection signal.** The fact that hqi16 was flagged
   *before its first broadcast* and never appeared in earlier operator
   notices means Cloudflare's classifier is fingerprinting on intrinsic
   features (NS pair `maria/mark.ns.cloudflare.com`, Gname.com Pte. Ltd.
   registrar, new-domain age, the specific Vite-built `/h5/js/index-*.js`
   asset paths, or some combination) — *not* on victim/community reports.
   The flag will continue to land on each new sibling within hours of
   first request, regardless of whether the domain is broadcast.

4. **Reserve consumption.** 16 domains pre-staged on 2026-05-03; 7 have been
   broadcast across 2026-05-13 (hqi22/23) and 2026-05-14 (hqi15/16/17/18/19);
   4 of 7 still working (hqi15, hqi17, hqi18, hqi19). At the observed
   ~daily rotation cadence with ~daily Cloudflare flagging, the remaining
   reserve (hqi20, hqi21, hqi25, hqi26, hqi27, hqi28, hqi29, hqi30, hqi31
   — 9 untested unflagged domains) provides on the order of two-to-three
   weeks of runway. Watch for: (a) a fresh batch from Gname.com Pte. Ltd.
   behind Cloudflare `maria/mark` NS pair, or (b) a CDN pivot away from
   Cloudflare entirely — the latter being the more interesting signal as
   it would indicate operators have diagnosed the classifier as their
   bottleneck.
